<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/content_list"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context="application.knhash.ddnews.ListActivity">

    <ListView
        android:layout_width="match_parent"
        android:layout_height="fill_parent"
        android:divider="@android:color/transparent"
        android:id="@+id/international_listview"
        app:layout_constraintBottom_toBottomOf="@+id/content_list"
        app:layout_constraintLeft_toLeftOf="@+id/content_list"
        app:layout_constraintRight_toRightOf="@+id/content_list"
        app:layout_constraintTop_toTopOf="@+id/content_list"/>

</android.support.constraint.ConstraintLayout>
